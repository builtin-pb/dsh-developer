# Close the loop with the user

Review the completed development task, including failed attempts and workarounds, not just the last passing command. Distinguish friction in dsh-developer from the target plugin, DSH, the environment or your own mistakes. State uncertainty when the cause is unclear. Repair authorized problems before reporting; a repaired product defect can still deserve a report. Do not invent friction or praise, and do not treat passing tests as proof that the whole experience was smooth.

Keep the user's requested outcome first. Add a short observation about consequential friction or a smooth run with a concrete basis, such as source and archive verification succeeding without a workaround. Do this at task completion or a genuine blocker, not after every tool call. Respect a request to omit feedback; declining a report must not interrupt the work or trigger repeated requests.

For actionable dsh-developer friction, prepare a concise proposed issue for **builtin-pb/dsh-developer**. Include the goal, expected and observed behavior, exact product/DSH versions and host, a minimal reproduction when known, and the workaround or remaining impact. Separate observations from hypotheses. State when reproduction is unavailable; do not manufacture it. Search public issues for an existing match when access is available. If one already covers the observation, share its link; propose a comment only when you have useful new evidence.

Show the proposed title and body to the user and ask whether to submit that specific report. Task execution is not permission to publish. Never attach repository source, session transcripts, local paths, credentials or user-identifying details by default; use synthetic examples and minimal sanitized excerpts. If the cause is upstream, explain that and seek approval for the actual upstream destination instead. If approval for this exact report and destination already exists, do not ask again.

After approval, use an available GitHub tool or authenticated `gh`. Supply multiline text through a structured body argument or `--body-file`, and specify the repository explicitly. Check the response and return the issue link. If an attempt has an uncertain outcome, check for the created issue before retrying. If submission is unavailable, leave the draft for manual submission without claiming it was filed. Do not install tools, change authentication, open browser windows or send automatic telemetry merely to report friction.
