/** The Host entry makes this package discoverable by DSH's client module registry. */
export const name = 'session-status';

/** All behavior belongs to the Client; no Host services or configuration are needed. */
export function apply(): void {}
